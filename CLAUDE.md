# 프로젝트 원칙: 데이터 소스 승인 기준

새로운 외부 데이터 소스(API, 스크레이핑 대상)를 추가하기 전, 아래 순서로 반드시 검토할 것.

1. robots.txt 확인
   - 전체 차단(Disallow: /)이거나 AI 봇(ClaudeBot, GPTBot, anthropic-ai 등)을 이름으로 지목해 차단한 경우 → 사용 금지
   - 일부 경로만 제한하는 수준이면 → 그 경로만 피해서 진행 가능

2. 공식 무료 API 우선
   - 무료 공개 API가 있으면(DART, ECOS, 공공데이터포털 등) 스크레이핑보다 최우선으로 사용
   - API 없으면 스크레이핑 검토, 단 호출 빈도는 마스터 스케줄러 주기(15분) 이상으로 촘촘하게 만들지 말 것

3. 기존 소스로 대체 가능한지 먼저 확인
   - 새 사이트를 붙이기 전에, 이미 쓰는 소스(네이버 등) 안에 같은 데이터가 있는지부터 확인

4. 요약/재구성 콘텐츠 생성 시 저작권 원칙
   - 특정 기사 하나를 그대로 요약/재가공하지 말 것
   - 여러 소스의 팩트를 종합해서 새로운 관점으로 재구성할 것
   - 원문 문장을 그대로 옮기지 말고 전부 패러프레이징할 것

5. tax/승계/재테크 등 민감 카테고리 콘텐츠 원칙
   - 구체적 세액, 수익률 예측, 투자 추천으로 읽힐 수 있는 단정적 표현 지양
   - "정보 제공" 톤 유지, 필요시 "일반적인 정보이며 개별 상황에 따라 다를 수 있다" 식의 안내 포함

6. 수치 인용 원칙
   - 프롬프트에 제공된 자료에 실제로 있는 숫자만 사용할 것, 없는 숫자를 만들어내지 말 것
   - 다만 원본 데이터 자체가 비정상적으로 크거나 이상해 보이는 값(예: 지수가 하루 만에
     10%p 이상 움직이는 등)을 옮겨야 할 때는, 그 숫자가 어느 자료에서 왔는지 코드 리뷰
     단계에서 원본 대조로 먼저 검증할 것 - AI가 지어낸 값인지 원본 데이터 자체의 값인지
     구분하지 않고 넘어가지 말 것
   - 등락률이 절대값 기준 ±5%를 넘는 이례적 수치를 인용할 때는, 숫자만 던지고 끝내지
     말고 왜 이런 변동이 나왔는지 맥락(전일 서킷브레이커, 미국 증시 연동, 특정 기업
     실적 등 자료에 근거가 있는 내용)을 한 문장 이상 함께 언급할 것. 또한 "이례적",
     "변동성이 큰 장세" 같은 표현으로 독자가 이 수치를 평상시 수준으로 오해하지
     않도록 신호를 줄 것
   - 과거 전체 기록과 비교하는 느낌을 주는 표현("역대", "최고", "최대",
     "사상 처음", "기록적" 등 이런 유형 전부 포함, 이 목록에 없는 비슷한
     표현도 마찬가지)은, 실제로 비교 가능한 과거 데이터가 자료에 함께
     주어져서 그 비교가 사실로 확인된 경우에만 쓸 것. 오늘 수치가 아무리
     커도 그것만으로 이런 단정적 표현을 쓰지 말 것 - 비교 대상 과거 기록이
     자료에 없으면 이런 유형의 표현 자체를 쓰지 말 것

# 프로젝트 원칙: 타이포그래피 표준

`widgets/news-widget.html` 등 프론트엔드 위젯에 새 탭/컴포넌트를 추가하거나
기존 텍스트 스타일을 만질 때는, 개별 요소마다 임의로 폰트 크기를 정하지
말고 아래 표준(쏘왓레터 뉴스레터 본문 스타일 기준)을 따를 것. 위젯의
`#news-widget` CSS 변수로 이미 정의돼 있으므로 새 요소도 인라인 스타일이
아니라 이 변수를 참조할 것.

- 폰트 스택: `'Pretendard','Helvetica Neue','Helvetica','Arial','Apple SD Gothic Neo','Noto Sans KR','Malgun Gothic',sans-serif`
  (`--nw-font-stack`)
- 제목(기사/항목 제목 등 title·headline 계열): weight 600, 20px(최초
  확정치는 22px였으나 실제 렌더링 확인 후 20px로 축소 확정),
  letter-spacing -0.04em, line-height 26px, color #1A1A1B
  (`--nw-title-weight/-size/-ls/-lh/-color`)
- 본문(body·summary·insight 계열): weight 400, 18px, letter-spacing -0.02em,
  line-height 28px, color #1A1A1B (`--nw-body-weight/-size/-ls/-lh/-color`)
- 강조 박스("SO WHAT" 성격의 인사이트/핵심 포인트 영역, 예:
  `overall_sowhat`): 배경 #FDF7F7(`--nw-sowhat-bg`), 타이틀 색상
  #d12f30(`--nw-sowhat-title-color`), 본문은 위 "본문" 스타일 그대로.
  일반 AI 브리핑/코멘트 박스(파란 톤)는 이 배색 대상이 아님 - 순수
  "SO WHAT"류 강조 영역에만 적용
- 링크: color #005AFF (`--nw-link-color`)
- 탭 버튼, 카테고리 뱃지, 타임스탬프, 섹션 소제목, 페이지 제목
  (`.nrw/.ew/.sw-title` 등), 수치값처럼 UI 구조/네비게이션 요소는 이
  표준 대상이 아님 - 뉴스 기사·항목의 실제 제목/본문 콘텐츠에만 적용

# 프로젝트 원칙: 레이아웃 확정 사항

`widgets/news-widget.html`에서 아래 배치/스타일은 이미 논의 끝에 확정된
상태다. 전역 스타일(타이포그래피 등)이나 레이아웃을 바꾸는 작업을 시작하기
전에 반드시 이 섹션을 먼저 확인하고, 아래 항목은 그 작업 범위에서 제외할
것 - 과거에 타이포그래피 일괄 적용 작업 중 이 부분이 실수로 함께 바뀌어
유실된 적이 있어 명문화한다.

- **`.pw-calendar-name`(정책·캘린더 탭의 "다가오는 경제 일정" 항목명)은
  새 타이포그래피 제목 표준(`--nw-title-size`) 적용 대상에서 제외한다.**
  날짜 컬럼이 고정폭인 가로 배치 행이라 22px을 적용했을 때 2~3줄로
  어색하게 줄바꿈되며 깨졌던 전례가 있다. 가독성 확대 요청으로
  14px→16px까지는 별도로 키웠지만(레이아웃 안전 확인됨), title 표준
  변수를 그대로 참조하지는 않는다 - weight 600, `var(--nw-text-sub)`
  색상 유지.
- **정책·캘린더 탭 "거시경제 지표"(`#pw-macro-indices`) 카드와 "다가오는
  경제 일정"(`#pw-calendar-list`) 섹션만 폰트가 확대돼 있다** (지표
  카드: `.sw-index-name` 13px/`.sw-index-value` 19px/`.sw-index-change`
  13px, `#pw-macro-indices` 안에서만 스코프 - 증권/주식 탭이 공유하는
  `.sw-index-*` 베이스 규칙은 건드리지 않음. 일정 날짜: `.pw-calendar-date`
  16px). 그 아래 주요공시/정부지원사업/소상공인 섹션은 확대 대상이
  아니다 - 이 두 섹션만 별도로 요청받아 키운 것이므로 전역 작업 시
  같이 건드리지 말 것.
- **정책·캘린더 탭 섹션 순서와 배치**:
  1. 거시경제 지표(`#pw-macro-indices`) - 탭 최상단에 배치
  2. 다가오는 경제 일정(`#pw-calendar-list`)
  3. CEO 캘린더(`#pw-cal-widget`) - 세무/노무/공휴일/DART M&A 통합 달력
  4. 주요 공시 DART(`#pw-dart-list`)
  5. 정부지원사업(`#pw-support-list`)
  6. 소상공인 정책자금(`#pw-soso-list`)

  2, 4~6번 섹션은 `.pw-2col` 클래스로 PC에서 2열, 모바일(`max-width:760px`)에서
  1열로 전환된다. 각 섹션은 기본 6개(`PW_INITIAL_COUNT`)만 노출하고, 6개를
  넘으면 하단에 "더보기" 버튼(`renderPwList` 공통 헬퍼, `.nrw-more-btn`
  스타일 재사용)이 나타나 전체를 펼쳐볼 수 있다 - 항목이 많아도 스크롤이
  과도하게 길어지지 않게 하기 위함. 거시경제 지표(`.pw-macro-grid`)와
  CEO 캘린더는 더보기 대상이 아니다(거시경제 지표는 고정 개수 요약 카드,
  CEO 캘린더는 자체 월별 그리드 + "다가오는 마감/최근 공시" 8개 고정
  노출 방식이라 이 패턴을 쓰지 않음).
- **CEO 캘린더(`#pw-cal-widget`, 3번 섹션, 구 "세무·노무 일정 캘린더")**:
  클래스명은 `pw-cal-` 접두사로 통일해 기존 `pw-calendar-*`(2번 "다가오는
  경제 일정") 클래스와 절대 겹치지 않게 했다 - 이름이 비슷해 보여도 서로
  다른 컴포넌트이므로 혼동하지 말 것. 필터 탭은 전체/세무/노무/M&A 4개.
  색상·폰트는 새 값을 만들지 않고 기존 `--nw-*` 변수(`--nw-line`,
  `--nw-surface`, `--nw-navy`, `--nw-text-sub`, `--nw-title-color`,
  `--nw-body-color`, `--nw-shadow-sm` 등)를 그대로 재사용한다. 세무(파란
  `#3182F6`)/노무(주황 `#F5A623`)/M&A(보라 `#8B5CF6`) pill과 공휴일 셀
  배경(연한 빨강)만 이 컴포넌트 전용 고정 색상이고, 이 색들은 CEO 리포트
  탭의 카테고리 뱃지 팔레트(`CEO_BADGE_COLORS`)와는 별개로 하드코딩된
  값이니 나중에 전역 색상 체계를 바꿀 때 같이 확인할 것.
  - **데이터 소스가 두 갈래로 완전히 분리돼 있다는 점이 이 위젯에서
    가장 중요한 설계 원칙이다.** 세무/노무/공휴일은 `dashboard.json`의
    `tax_labor_events`/`tax_labor_source_status`를 대시보드 최초 로딩
    시점에 이미 fetch된 걸 그대로 쓴다(`scraper_tax_calendar.py`가 생성).
    반면 M&A(DART 시그널)는 `data/dart_ma_signals.json`을 **"M&A" 필터
    탭을 사람이 처음 클릭하는 그 순간에만" `pwCalLoadMaSignals()`로
    지연 로딩(lazy fetch)한다 - 전체/세무/노무 탭만 쓰는 사용자는 이
    fetch가 평생 한 번도 안 일어날 수 있다. 한 번 로드되면
    `pwCalMaLoaded` 플래그로 메모리에 캐싱해서 다시 요청하지 않는다.
    `dart_ma_signals.json`은 `dashboard.py`(`dashboard.json`)에 **절대
    합치지 않는다** - 대시보드 최초 로딩 속도(모든 탭 전환)에 DART
    M&A 데이터가 조금이라도 영향을 주지 않게 하기 위함이며, 이 원칙을
    깨고 dashboard.json에 합치는 리팩터링은 하지 말 것.
  - "다가오는 마감"/"최근 공시" 리스트도 필터에 따라 완전히 다른 로직으로
    갈린다: 전체/세무/노무는 `pwCalRenderDeadlines()`(미래 날짜만, D-day
    배지, 오름차순, 클릭 시 모달), M&A는 `pwCalRenderRecentFilings()`(이미
    발생한 공시라 미래 필터 없음, 카테고리 배지, 날짜 내림차순, 클릭 시
    모달이 아니라 DART 원문 링크로 바로 새 탭 이동)로 별개 함수다 - 같은
    리스트처럼 보이지만 성격이 다르므로 억지로 하나의 함수로 합치지 말 것.

# 프로젝트 원칙: 작업 완료 기준

`widgets/news-widget.html`을 수정하는 작업은 다음 조건을 모두 만족해야
"완료"로 본다 - 커밋/푸시했다는 사실만으로 끝난 게 아니다.

- 작업 완료를 보고하는 바로 그 응답 안에서, 사용자가 별도로 "소스 줘"라고
  다시 요청하지 않아도 되도록 **파일 전체 내용을 바로 제공**할 것. 파일이
  작아서 채팅에 안전하게 통째로 옮겨 적을 수 있는 경우가 아니면(대부분
  이 파일은 수천 줄이라 해당 안 됨), artifact로 발행해서 "복사 버튼으로
  바로 복사해 아임웹 커스텀 HTML에 붙여넣을 수 있는 상태"까지 만들어
  링크를 제공할 것. 실제 저장된 파일을 그대로 옮기고(재입력 금지, cat 등으로
  가져와 붙여넣기), 원격 raw URL만 안내하고 끝내지 말 것 - 사용자가
  "다운로드 링크 말고" 라고 명시적으로 요청한 적이 있다.
- "커밋 완료했습니다"로 응답을 마치지 말 것. 그다음 단계(복사해서
  아임웹에 붙여넣기)까지 사용자가 추가 요청 없이 바로 이어갈 수 있는
  상태로 응답을 끝낼 것.
- artifact를 갱신할 때는 매번 새 URL을 만들지 말고, 같은 파일 경로로
  재발행해서 기존 링크를 그대로 유지할 것(사용자가 북마크해서 계속 같은
  주소로 확인할 수 있도록).
